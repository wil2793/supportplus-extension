PORT=3500
DB_SERVER=host.docker.internal
DB_NAME=SupportPlusDB
DB_USER=sa
DB_PASSWORD=your_password_here
DB_PORT=1433

# API Authentication (optional — leave empty for development/no auth)
# When set, all requests must include header: X-API-Key: <value>
SP_API_KEY=
